let form = document.getElementById("form")
let fullNameInput = dpcument.getElementById("fullname")

form.addEventListener("submit",function(event){

    event.preventDefault()
    let userFullName = fullNameInput.value
} )